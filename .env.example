# OYD Foundry Migrator Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Optional Configuration
# =============================================================================

# Configuration directory (default: ~/.oyd-migrator)
# OYD_MIGRATOR_CONFIG_DIR=~/.oyd-migrator

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# OYD_MIGRATOR_LOG_LEVEL=INFO

# Log file path (optional, logs to stderr by default)
# OYD_MIGRATOR_LOG_FILE=/var/log/oyd-migrator.log

# Disable colored output (default: false)
# OYD_MIGRATOR_NO_COLOR=false

# Default Azure region for new resources (default: eastus)
# OYD_MIGRATOR_DEFAULT_LOCATION=eastus

# =============================================================================
# Azure Authentication (if not using Azure CLI)
# =============================================================================

# Service Principal authentication
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret

# User-assigned Managed Identity (for Azure-hosted environments)
# AZURE_CLIENT_ID=your-managed-identity-client-id

# =============================================================================
# Pre-configured Resources (optional)
# =============================================================================

# Azure Subscription ID (will prompt if not set)
# AZURE_SUBSCRIPTION_ID=your-subscription-id

# Default Foundry Project endpoint
# FOUNDRY_PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project

# Default model deployment
# FOUNDRY_MODEL_DEPLOYMENT=gpt-4.1

# =============================================================================
# Azure AI Search (for manual configuration)
# =============================================================================

# Search service endpoint
# AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net

# Search admin key (only needed if not using managed identity)
# AZURE_SEARCH_API_KEY=your-api-key
